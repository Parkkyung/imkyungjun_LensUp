# LensUp

> 공공 민원 데이터를 수집·정제하고 AI로 분석하여 지역별 주요 이슈와 변화 흐름을 제공하는 웹서비스

🔗 **배포 사이트:** https://imkyungjun-lens-up.vercel.app/

---

## 프로젝트 소개

LensUp은 공공 민원 데이터를 활용해 반복적으로 나타나는 키워드와 지역별 이슈를 분석하고,
그 결과를 대시보드·지도·검색 화면으로 제공하는 캡스톤디자인 팀 프로젝트입니다.

공공데이터 API에서 민원 데이터를 수집한 뒤 공통 형식으로 정리하고,
AI 분석 결과를 JSON Payload로 구성해 FastAPI 서버와 PostgreSQL에 전달합니다.

---

## 핵심 개선 결과

| 개선 항목 | 개선 전 | 개선 후 | 결과 |
|---|---:|---:|---|
| 지도 GeoJSON 파일 | 약 78.5MB | 약 0.54MB | 파일 크기 약 **99.3% 감소** |
| 외부 API 대량 요청 | `502 Bad Gateway`, `504 Gateway Timeout` 반복 | 요청량 축소 및 분할 수집 | 필요한 민원 데이터 수집 완료 |

### GeoJSON 경량화

지도 화면에서 사용하는 GeoJSON 파일이 커 로딩이 지연되는 문제가 있었습니다.
서비스에 불필요한 속성을 제거하고 경계 좌표를 단순화해 지역 경계는 유지하면서
파일 크기를 약 **78.5MB에서 0.54MB로 줄였습니다.**

### 외부 API 오류 대응

많은 데이터를 한 번에 요청할 때 외부 Open API에서 `502`, `504` 오류가 반복됐습니다.
수집 코드·요청값·JSON 생성 과정을 점검하고 API 운영기관에 오류 상황을 문의했습니다.
외부 서버 문제임을 확인한 뒤 한 번에 요청하는 데이터 수를 줄이고 여러 차례 나누어 수집하도록 수정해
필요한 데이터를 확보했습니다.

---

## 주요 기능

- 공공 민원 데이터 수집
- 기관별 API 응답 데이터 정제 및 공통 형식 변환
- 민원 관련 키워드 탐색
- AI를 활용한 민원 내용 요약 및 이슈 분석
- 분석 결과를 JSON Payload로 구성해 서버에 전달
- 지역별 민원 이슈 조회 및 검색
- 대시보드와 지도 기반 데이터 시각화

---

## 담당 역할

### 임지원 — Backend 1 · 데이터 수집 및 분석 파이프라인

- 공공데이터 API 연동 및 민원 데이터 수집
- 기관마다 다른 API 응답 형식 정규화
- AI 분석에 사용할 입력 데이터 구성
- Backend 서버 전송용 JSON Payload 생성
- 팀원과 필드명·데이터 형식을 조율하고 연동 테스트 진행
- 외부 API `502`·`504` 오류 분석 및 분할 수집 방식 적용
- 지도 GeoJSON 경량화를 통한 로딩 데이터 크기 개선


### 박경수 — Backend 2 · DB 및 서비스 API

- FastAPI 기반 백엔드 서버 및 라우터 구성
- PostgreSQL · SQLAlchemy 기반 DB 연결 및 모델 설계
- Backend 1 분석 결과 수신용 Pipeline API 구현
- 이슈·키워드·트렌드·지역 순위 데이터 DB 저장
- 대시보드 목록·상세 및 지도 분석 API 구현
- 통합 검색·자동완성·이슈 조회 API 구현
- 프론트엔드 연동을 위한 응답 데이터 구조 및 Payload 구성
---

## 기술 스택

### Data Pipeline
`Python` `REST API` `JSON` `OpenAI API`

### Backend
`FastAPI` `PostgreSQL` `SQLAlchemy`

### Frontend
`React` `Vite`

---

## 프로젝트 구조

```text
imkyungjun_LensUp/
├── BE1_source_code/    # 데이터 수집·정제·AI 분석 및 서버 전송
├── BE2_source_code/    # FastAPI 서버 및 PostgreSQL 데이터베이스
├── FE_source_code/     # React 웹 화면
└── README.md
```

---

## 데이터 처리 과정

```text
공공 민원 데이터 수집
        ↓
API 응답 정제 및 형식 통일
        ↓
키워드 탐색 및 AI 분석
        ↓
JSON Payload 생성
        ↓
FastAPI 서버 전달 및 DB 저장
        ↓
대시보드·지도·검색 화면 제공
```

---

## 프로젝트를 통해 경험한 점

- 외부 API 연동 시 내부 코드와 외부 환경을 구분해 오류 원인을 확인하는 과정
- 서로 다른 응답 데이터를 서비스에서 사용할 수 있는 공통 구조로 변환하는 과정
- 분석 파이프라인과 백엔드 서버 사이의 데이터 전달 방식
- 대용량 지도 데이터를 실제 사용 목적에 맞게 경량화하는 과정
- 팀원과 데이터 필드와 전달 형식을 맞추며 하나의 서비스로 통합하는 협업 과정
